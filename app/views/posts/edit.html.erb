<%= form_for @post do |f| %>
  
  <div>
    <%= f.label :title %>
    <%= f.text_field :title, :value => @post.title %>
  </div>

  <div>
    <%= f.label :content %>
    <%= f.text_field :content, :value => @post.content %>
  </div>

  <%= f.submit %>
<% end %>